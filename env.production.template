# ============================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ============================================
# Copy this file to .env and update with your actual values
# IMPORTANT: Never commit .env file to git!

# Application Environment
NODE_ENV=production
PORT=3000

# ============================================
# JWT CONFIGURATION
# ============================================
# Generate a strong random secret for production
# You can generate one using: openssl rand -base64 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# CORS CONFIGURATION
# ============================================
# Specify allowed origins separated by commas
# Example: "https://yourdomain.com,https://www.yourdomain.com"
# Leave as "*" to allow all origins (credentials will be disabled)
CORS_ORIGINS=*

# ============================================
# DATABASE CONFIGURATION (Neon PostgreSQL)
# ============================================
# IMPORTANT: For Neon, you need TWO different connection strings:
#
# 1. DATABASE_URL: Use the POOLER endpoint (recommended for regular connections)
#    - Hostname contains "-pooler" (e.g., ep-xxx-pooler.region.aws.neon.tech)
#    - Better for connection pooling and serverless environments
#
# 2. DIRECT_URL: Use the DIRECT endpoint (required for migrations)
#    - Hostname does NOT contain "-pooler" (e.g., ep-xxx.region.aws.neon.tech)
#    - Required for Prisma migrations (db push, migrate deploy, etc.)
#
# Get both connection strings from Neon Console:
# https://console.neon.tech -> Your Project -> Connection Details
#
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE?sslmode=require

# Pooler connection (for regular queries)
DATABASE_URL="postgresql://neondb_owner:YOUR_PASSWORD@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# Direct connection (for migrations)
DIRECT_URL="postgresql://neondb_owner:YOUR_PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# ============================================
# NOTES
# ============================================
# - Replace YOUR_PASSWORD with your actual database password
# - Replace ep-xxx-pooler.region.aws.neon.tech with your actual pooler hostname
# - Replace ep-xxx.region.aws.neon.tech with your actual direct hostname
# - Replace neondb_owner with your actual database username if different
# - Replace neondb with your actual database name if different
#
# After creating .env file:
# 1. Run: npm run prisma:generate (to generate Prisma client)
# 2. Run: npm run prisma:deploy (to run migrations)
# 3. Run: npm run build (to build the application)
# 4. Restart PM2: pm2 restart backend


